# PostgreSQL Database (Dokploy internal — same container network)
DATABASE_URL=postgresql://postgres:3coinsltd@rpnmore-rpnmore-ktcg1a:5432/rp?sslmode=disable

# Gemini AI (for blog generation)
GEMINI_API_KEY=your_gemini_api_key

# Firebase Admin (JSON string)
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account",...}

# Admin login
ADMIN_PASSWORD=rpnmore-admin-2026

# Environment
NODE_ENV=production